import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.beyondhorizonwebsite',
  appName: 'beyondhorizonwebsite',
  webDir: 'www'
};

export default config;
